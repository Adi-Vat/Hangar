using UnityEngine;

public class ShipOutputComponent : MonoBehaviour
{
    public float inputValue;
    public Transform inputJack;

    //[SerializeField]
    //axis inputAxis;

    enum axis
    {
        x,y,z
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
